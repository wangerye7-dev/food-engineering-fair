
const f=document.getElementById('coopForm');
f.addEventListener('submit',e=>{e.preventDefault();const types=[...f.querySelectorAll('[name="types"]:checked')].map(x=>x.value);if(!types.length){toast('请至少选择一个合作方向');return}if(!f.reportValidity())return;const fd=new FormData(f);const record={id:uid('COOP'),createdAt:new Date().toISOString(),company:fd.get('company'),contact:fd.get('contact'),position:fd.get('position'),phone:fd.get('phone'),types,detail:fd.get('detail')};const list=getStore(KEYS.cooperation);list.push(record);setStore(KEYS.cooperation,list);f.innerHTML=`<div class="success-box" style="margin:0"><div class="success-icon">✓</div><h2>合作需求已登记</h2><p class="muted">登记编号：${record.id}</p><p>学院后续将根据您提供的信息进行分类对接。</p><a class="btn btn-primary" href="index.html">返回首页</a></div>`});
